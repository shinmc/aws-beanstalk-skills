{
  "description": "Auto-approve AWS Elastic Beanstalk CLI calls",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-approve-aws.sh"
          }
        ]
      }
    ]
  }
}
